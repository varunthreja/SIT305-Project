using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockPickUp : MonoBehaviour {
    
    //private void OnCollisionEnter(Collision other)
    //{
    //    if(other.gameObject.tag == "Player")
    //    {
    //        GetComponent<BoxCollider>().enabled = false;
    //        GetComponent<MeshRenderer>().enabled = false;
    //        GamePlay.instance.BlockPickupSound.Play();
    //        PlayerPrefs.SetInt("Blocks", PlayerPrefs.GetInt("Blocks") + 2);
    //        GamePlay.instance.BlocksCountText.text = PlayerPrefs.GetInt("Blocks").ToString();
    //    }
    //}
}
